import React from "react";

const FormViewTitle = ({ title }) => {
    return (
        <div className="form-view-title">
            <p>{title}</p>
        </div>
    );
};

export default FormViewTitle;
